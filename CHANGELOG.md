## 2.5.20

- Для кнопки и заголовка «Справочники» добавлен отдельный emoji-слот `dictionaries` с fallback 📚; premium emoji можно назначить в справочнике эмодзи независимо от «Хостеров» и «Настроек».
- В списке хостеров каждая кнопка использует emoji «Хостер», а в списке тегов - emoji «Теги».
- В уведомлениях о ближайшей/просроченной оплате показывается заметка VPS, если она заполнена.

## 2.5.19

- В уведомлении о восстановлении VPS теперь показывается хостер.
- Исправлен вводящий в заблуждение вывод TCP 22 в режиме «Авто»: если Ping успешен и TCP-проверка не запускалась, бот пишет «не проверялся (Ping OK)», а не «недоступен».
- TCP-диагностика теперь различает успешное соединение, TCP RST/закрытый порт, таймаут и сетевую ошибку. TCP RST по-прежнему считается признаком доступного VPS при fallback-проверке.
- Добавлены regression-тесты для Auto/Ping/TCP диагностики и проверки TCP 22.

## 2.5.18

- В настройках напоминаний пресеты времени изменены на 10:00, 15:00 и 20:00 в одной строке.
- В справочниках стран и тегов добавлено удаление с обязательным подтверждением и списком затронутых VPS.
- При удалении страны поле «Страна» очищается у связанных VPS; при удалении тега удаляется только выбранный тег, остальные сохраняются.
- В меню команд Telegram оставлена только команда /start с описанием Main menu.
- Служебные клавиатуры удаления и возврата унифицированы в одноколоночный вид; точную ширину inline-кнопок по-прежнему определяет клиент Telegram.

## 2.5.17

- Кнопка «Имя» в данных VPS теперь зелёная.
- В разделе архива используется та же premium-иконка, что и у кнопки архива.
- Месячный отчёт: дни 1, 3, 5 и 10 отображаются одной строкой; время 10:00, 15:00 и 20:00 - одной строкой.
- Ручной ввод дня и времени отчёта объединён в одну кнопку; вместо «Отмена» используется «Возврат».
- В справочнике хостеров кнопка «Открыть ЛК» использует premium emoji перехода в ЛК.
- Кнопка «Удалить старые бекапы» остаётся красной и удаляет только локальные .db и .tar.gz бекапы старше 30 дней; более свежие резервные копии сохраняются.
- Тексты бекапов приведены к русским названиям: «Создать и прислать бекап», «Удалить старые бекапы»; в разделе явно указано правило хранения 30 дней.
- В «Ближайших оплатах» показывается заметка VPS, если она заполнена.

## 2.5.16

- Добавлено изменение имени VPS из раздела «Данные».
- Из карточки «Данные VPS» удалена подсказка «Нажми поле, чтобы изменить. Для очистки отправь -».
- При редактировании IP и других текстовых полей кнопка «Отмена» заменена на белую «К карточке»; возврат корректно завершает режим редактирования.

## 2.5.15

- Исправлено обновление VPS Bill из Telegram на существующих установках: каталог `update-requests` автоматически получает владельца UID/GID `10001:10001` и права на запись для контейнера.
- Исправлен `Permission denied: /app/update-requests/.request.json.tmp`: бот больше не использует фиксированное имя временного request-файла и создаёт уникальный атомарный temp-файл.
- `install.sh`, штатный updater и host-side update bridge теперь сами восстанавливают права каталога обновлений и удаляют только зависшие временные request-файлы.
- Bind mount `/app/update-requests` явно помечен как writable (`rw`).
- Добавлены regression-тесты для writable update bridge и атомарного создания `request.json`.

## 2.5.14

- Release notes в Telegram теперь загружаются строго из `CHANGELOG.md` соответствующего GitHub tag.
- GitHub autogenerated notes и строка `Full Changelog` больше не используются ботом.
- Скрипт публикации релиза теперь требует секцию текущей версии в `CHANGELOG.md`; без неё публикация останавливается.
- GitHub Release получает только содержимое секции текущей версии из `CHANGELOG.md`.
- Исправлена синхронизация версии в `.env.example` при сборке релиза.

## 2.5.13

- Исправлена кнопка «Сбросить все» в настройках premium emoji: callback с номером страницы теперь корректно обрабатывается.
- Добавлены штатные premium emoji `cabinet` и `update` в дефолтный набор VPS Bill.
- Добавлен regression-test для сброса emoji с пагинацией.

## 2.5.12

- Исправлен парсинг версии GitHub Release в `vps-bill-update`: теги `v2.5.12`, `v.2.5.12`, `2.5.12` и `.2.5.12` нормализуются в `2.5.12`.
- `vps-bill-update` без аргументов теперь сам проверяет последний GitHub Release `flu-ghsh/vps-bill`, находит asset `vps-bill-X.Y.Z.tar.gz` и обновляет через штатный безопасный pipeline.
- Добавлена строгая проверка версии и совпадения версии внутри архива с GitHub tag.
- Поддержана проверка SHA256 через GitHub asset digest, `.sha256` или `SHA256SUMS`, если они опубликованы.
- Исправлена такая же нормализация версии в Telegram-механизме обновления и host-side update bridge.

## 2.5.11

- Исправлено открытие карточек VPS из списка серверов и уведомлений.
- Архив VPS теперь всегда виден в разделе «Серверы», даже если он пуст.
- Исправлен premium emoji «Обновление»: один слот используется в кнопке и заголовках раздела.
- Пагинация справочника emoji сохранена по 8 слотов на страницу.

## 2.5.10

- Исправлено открытие карточек VPS из списка серверов и из уведомлений: новые кнопки используют отдельный callback `srvopen`, старые `server:` остаются совместимыми.
- Добавлена пагинация Premium emoji по 8 слотов на страницу.
- Добавлен отдельный emoji-слот `Обновление`.
- После изменения emoji бот возвращается на ту же страницу списка.

## 2.5.9

- Автоматическая установка обновлений по systemd timer удалена.
- Добавлено ручное обновление VPS Bill прямо из Telegram через безопасный host-side bridge без Docker socket в контейнере.
- `Настройки → Обновление` показывает установленную и доступную версии и позволяет проверить GitHub Releases вручную.
- Раз в сутки, за 30 минут до времени платёжных уведомлений, бот проверяет GitHub Releases и предлагает новую версию, но не устанавливает её без подтверждения.
- Для найденной версии доступны «Установить», «Напомнить завтра» и «Не напоминать об этой версии».
- После host-side обновления бот присылает результат в Telegram.
- Старый `vps-bill-auto-update.timer` автоматически отключается и удаляется.
- `.env` очищен от пользовательских настроек emoji: premium/custom emoji управляются только через Telegram и SQLite.

## 2.5.8

- Hotfix запуска: восстановлены обработчики разделов «Периоды», «Валюты» и «Справочники», которые отсутствовали в 2.5.7 и приводили к падению BotHandlers при старте.
- Добавлен regression-test, который проверяет, что каждый зарегистрированный callback/message handler реально существует.
- Все функции 2.5.7 (Архив VPS, очистка backup и автообновление) сохранены.

## 2.5.7

- Убрана кнопка «Что нового» из настроек.
- Добавлен отдельный архив VPS: архивный сервер больше не участвует в напоминаниях и мониторинге, но его история платежей сохраняется в аналитике.
- Из архива VPS можно вернуть в активные или переместить в корзину.
- Добавлена ручная и автоматическая очистка старых backup: по умолчанию хранятся максимум 30 файлов и не старше 30 дней.
- Добавлено безопасное автообновление через host-side systemd timer без проброса Docker socket в контейнер.
- Автообновление использует штатный безопасный updater: manifest + SHA256, backup, тестовая миграция, health-check и rollback.

## 2.5.5

- Личный кабинет хостера принимает обычный сайт, `t.me/...` или `@telegram_bot`.
- В «Ближайших оплатах» у хостера со ссылкой появился отдельный emoji перехода в ЛК.
- В аналитике названия хостеров не являются ссылками.
- Добавлен `CHANGELOG.md` для GitHub Releases.

## 2.5.4

- Новый формат ближайших оплат.
- Ссылки на личный кабинет хостера.
- Настраиваемое время уведомлений об оплате.
- «Бекап», «Отчёт» и «Периоды» получили более компактный интерфейс.
- В отчёте добавлен ручной ввод дня и времени.

## 2.5.3

- Premium emoji пользователя стали штатным набором VPS Bill по умолчанию.

## 2.5.2

- Нейминг проекта унифицирован до VPS Bill / vps-bill.
- Backup-файлы получили префикс `vps-bill-`.

## 2.5.1

- Мониторинг получил режим Auto: ICMP с fallback на TCP.

## 2.5.0

- Stability release: persistent outbox, защита от двойной оплаты, параллельный мониторинг, heartbeat и безопасный restore.
